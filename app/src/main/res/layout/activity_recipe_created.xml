<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.cookhelper.cookhelper.RecipeCreated"
    tools:textAlignment="center">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:gravity="center">


        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:text="Recipe Created"
            android:layout_weight="25"

            android:textSize="36sp"
            android:gravity="center"
            android:id="@+id/textTitle"
            android:adjustViewBounds="true"
            />

        <ImageView
            app:srcCompat="@drawable/checkmarkclipart"
            android:id="@+id/imgCheckmark"
            android:layout_weight="40"
            android:layout_gravity="center"
            android:layout_width="350dp"
            android:layout_height="0dp"
            android:cropToPadding="false"
            />

        <Button
            android:text="Back to Home"
            android:layout_width="698dp"
            android:id="@+id/btnHome"
            android:textSize="18sp"
            tools:text="Back to Home"
            android:layout_height="0dp"
            android:layout_weight="17"
            android:onClick="openHome (RecipeCreated)" />

        <Button
            android:id="@+id/btnRecipe"
            android:textSize="18sp"
            tools:text="Create Another Recipe"
            android:layout_width="698dp"
            android:layout_height="0dp"
            android:layout_weight="17"
            android:text="Create Another Recipe"
            android:onClick="openAdd" />
    </LinearLayout>

</RelativeLayout>
